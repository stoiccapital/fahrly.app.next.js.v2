# Environment Variables Template
# 
# This file is a template for environment variables used in Fahrly v2.
# 
# IMPORTANT:
# - Do NOT commit actual secrets or sensitive values
# - Do NOT use dummy values that look real
# - Add actual values as Vercel Environment Variables in the Vercel dashboard
# - Copy this file to .env.local for local development (gitignored)
#
# Public variables (accessible in browser):
# NEXT_PUBLIC_* variables are exposed to the browser
# Example: NEXT_PUBLIC_API_URL=https://api.example.com
#
# Private variables (server-only):
# These are only available on the server side
# Example: DATABASE_URL=postgresql://...
# Example: API_SECRET_KEY=...

# Currently, Fahrly v2 uses mock data and does not require environment variables.
# Add variables here as needed when integrating with external services.
